ULTRA SCROLL-DRIVEN 3D LUXURY CAR HOMEPAGE PROMPT

Enhance my existing luxury car selling homepage using Three.js or Spline.
The goal is NOT a simple rotating 3D object — but a cinematic scroll-based experience where the 3D car and text move together in sync as one story flow.

Do NOT redesign the full layout. Keep the structure, but upgrade interactions, motion, and immersion.

🎯 CORE OBJECTIVE

Create a luxury automotive website experience like a premium car brand (Ferrari / Lamborghini level) where:

The car changes position, angle, and scale as the user scrolls
The text content transitions in sync with the 3D object
The entire homepage feels like a guided cinematic journey
🚗 3D CAR BEHAVIOR (IMPORTANT)

The 3D car must NOT only spin.

Instead:

On scroll down → car moves forward in depth (Z-axis)
Next section → car rotates to show different angle (front → side → rear)
Mid scroll → slight zoom-in cinematic focus
Final section → car fades or moves aside to reveal CTA
Motion must be smooth using easing (no linear movement)

Add:

Camera movement synced with scroll
Parallax depth effect
Lighting shifts subtly as sections change
🧠 TEXT + UI FLOW (VERY IMPORTANT)

Text must move with the story:

Hero text fades in with car entrance
As user scrolls:
Old text fades out smoothly
New section text slides in (not instant)
Text position shifts slightly based on scroll (parallax feel)

Each section should feel like:
“Scene 1 → Scene 2 → Scene 3” instead of static blocks.

🎨 VISUAL STYLE
Dark luxury theme (black, deep gray, chrome accents)
Cinematic lighting (soft key light + rim light + reflections)
Smooth gradients in background (no flat colors)
Subtle dust/particles for depth

Materials:

glossy paint
reflective chrome
soft glow edges
⚡ ANIMATION STYLE RULES
Everything must be scroll-driven (not autoplay only)
Use easing (smooth interpolation)
No sudden jumps or harsh transitions
Motion must feel “premium slow cinematic”
📱 RESPONSIVENESS
Desktop: full scroll-driven 3D experience
Mobile: simplified transitions or reduced camera movement
💻 OUTPUT REQUIREMENTS

If using Three.js:

Use scroll event or ScrollTrigger logic
Sync:
camera position
car rotation
text transitions
Modular clean code

If using Spline:

Use scroll animations timeline
Bind object position + camera to scroll
Export embed-ready scene
✨ FINAL EXPERIENCE GOAL

The homepage should feel like:

A cinematic luxury car reveal experience
Where scrolling = controlling a story
3D object + text move as one synchronized system

Not a website. A luxury interactive showroom experience.
